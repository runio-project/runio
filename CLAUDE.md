# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Working Style

- Ask instead of assuming when not clear
- Bazzite and Aurora (preferred) repos are available locally for reference:
  - `~/Code/aurora`
  - `~/Code/bazzite`

## Project Overview

RunIO is a custom bootc (bootable container) image built on Universal Blue's Aurora (KDE Plasma on Fedora Atomic). The image is defined via a Containerfile and published to GitHub Container Registry.